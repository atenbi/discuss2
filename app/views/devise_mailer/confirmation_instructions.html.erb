<!DOCTYPE html>
<html>
<head>
  <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
  <style>
    body {
      font-family: Inter, Helvetica, Arial, sans-serif;
      background-color: #f8f9fa;
      margin: 0;
      padding: 0;
    }
    .container {
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    .header {
      background-color: #ffffff;
      border-radius: 8px 8px 0 0;
      padding: 24px;
      text-align: center;
    }
    .content {
      background-color: #ffffff;
      padding: 24px;
    }
    .footer {
      background-color: #ffffff;
      border-radius: 0 0 8px 8px;
      padding: 24px;
      font-size: 14px;
      color: #666666;
    }
    .logo {
      font-size: 28px;
      font-weight: 700;
      color: #000000;
      margin-bottom: 16px;
    }
    .divider {
      border-top: 1px solid #eaeaea;
      margin: 0 24px;
    }
    .title {
      font-weight: 600;
      font-size: 20px;
      line-height: 28px;
    }
    .text {
      font-size: 16px;
      line-height: 24px;
      color: #333333;
      margin-top: 16px;
    }
    .button {
      display: inline-block;
      background-color: #06b6d4;
      color: white;
      font-weight: 600;
      font-size: 16px;
      padding: 12px 24px;
      text-decoration: none;
      border-radius: 4px;
      margin-top: 16px;
    }
    .small-text {
      font-size: 14px;
      color: #666666;
      margin-top: 16px;
    }
    .link {
      color: #5A6B8F;
      font-weight: 500;
      word-break: break-all;
    }
    .button-container {
      text-align: center;
      margin-top: 16px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <div class="logo">discuss²</div>
      <div class="divider"></div>
    </div>
    <div class="content">
      <div class="title">Verify your email address</div>
      <div class="text">You're almost ready to start enjoying discuss². Please verify your email address to complete your registration.</div>
      <div class="button-container">
        <a href="<%= user_confirmation_url(confirmation_token: @token) %>" class="button">Verify email address</a>
      </div>
      <div class="small-text">If the button doesn't work, copy and paste this link into your browser:</div>
      <div class="link"><%= user_confirmation_url(confirmation_token: @token) %></div>
    </div>
    <div class="footer">
      <div class="small-text">If you did not create an account on discuss², please ignore this email. This verification link is only valid for 24 hours.</div>
    </div>
  </div>
</body>
</html>
