div class="flex items-center justify-center font-bold text-white" style="width: #{width}; height: #{height}; font-size: #{font_size}; border-radius: 50%; background-color: #{bg_color};" role="img" aria-label="Avatar for #{username}"
  = first_letter
