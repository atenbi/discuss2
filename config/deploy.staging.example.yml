service: your-app-staging

image: your-dockerhub-username/your-app-staging

servers:
  web:
    hosts:
      - YOUR_STAGING_SERVER_IP

proxy:
  ssl: true
  hosts: 
    - your-staging-domain.com
    - www.your-staging-domain.com

registry:
  username: YOUR_DOCKERHUB_USERNAME

  password:
    - KAMAL_REGISTRY_PASSWORD

env:
  secret:
    - RAILS_MASTER_KEY
  clear:
    RAILS_ENV: staging
    SOLID_QUEUE_IN_PUMA: true

aliases:
  console: app exec --interactive --reuse "bin/rails console"
  shell: app exec --interactive --reuse "bash"
  logs: app logs -f
  dbc: app exec --interactive --reuse "bin/rails dbconsole"

volumes:
  - "your_app_storage:/rails/storage"

asset_path: /rails/public/assets

builder:
  arch: amd64
